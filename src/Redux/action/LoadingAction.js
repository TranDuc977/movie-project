import { DISPLAY_LOADING, LOADING_API } from "../../utils/settings";
import { HIDE_LOADING, HIDE_API } from './../../utils/settings';

export const displayLoadingAction = {
    type: DISPLAY_LOADING
}

export const hideLoadingAction = {
    type: HIDE_LOADING
}

export const loadingApiAction = {
    type : LOADING_API
}

export const hideApiAction = {
    type : HIDE_API 
}